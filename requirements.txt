# Track 2 — chart-layer breadth test
# Core data/compute. Pinned loosely; tighten before deployment.
pandas>=2.0
numpy>=1.24
pyyaml>=6.0

# Parquet cache for loaders (data/*.parquet):
pyarrow>=12.0

# Data sources (install as needed per market):
requests>=2.28             # Twelve Data REST adapter (loaders/twelvedata.py)
breeze-connect>=1.0.59     # ICICI Breeze SDK — Indian instruments (loaders/breeze.py)

# Durable time-series store (OI chain / PCR summary / macro) — Railway Postgres via
# DATABASE_URL. Optional: unset DATABASE_URL falls back to parquet (local/offline).
psycopg2-binary>=2.9

# Live agent dashboards:
streamlit>=1.30            # dashboard/app.py (one-pane Streamlit fallback)
fastapi>=0.110            # web/server.py — flicker-free cockpit (JSON API)
uvicorn[standard]>=0.27   # ASGI server: uvicorn web.server:app
python-multipart>=0.0.6   # chat screenshot uploads (multipart form)

# Claude reasoning + sparring layer (agent/) — needs ANTHROPIC_API_KEY:
anthropic>=0.40            # official SDK; model claude-opus-4-8

# Dev / testing:
# pytest>=7.0
