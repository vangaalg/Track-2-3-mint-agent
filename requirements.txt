# Track 2 — chart-layer breadth test
# Core data/compute. Pinned loosely; tighten before deployment.
pandas>=2.0
numpy>=1.24
pyyaml>=6.0

# Data sources (install as needed per market):
# breeze-connect>=1.0      # ICICI Breeze — Indian instruments
# twelvedata>=1.2          # Twelve Data — global instruments

# Dev / testing:
# pytest>=7.0
